/*
============================================================
48 — DATABASE/SQL
Problem 11 — Count Users
============================================================

Count total users in the database.

Requirements:

    1. Ensure users table exists

    2. Create function:

           func countUsers(db *sql.DB) (int, error)

    3. Query:

           SELECT COUNT(*) FROM users

    4. Use QueryRow and Scan into int

    5. Print:

           Total users: <count>

    6. Insert at least one user if table is empty (optional seed)

============================================================
Goal
============================================================

Practice:

    - aggregate queries
    - COUNT(*)
    - QueryRow with single value

============================================================
*/

package main

func main() {}
