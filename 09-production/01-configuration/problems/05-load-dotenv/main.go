/*
============================================================
01 — CONFIGURATION
Problem 5 — Load .env with godotenv
============================================================

Load configuration from .env file for local development.

Requirements:

    1. Use github.com/joho/godotenv

    2. Try loading .env from repo root when running locally

    3. If .env not found → log message and continue (production style)

    4. After load, read DATABASE_URL and JWT_SECRET with os.Getenv

    5. Print whether each is set (do NOT print secret values)

    6. Run from repo root:

           go run ./09-production/01-configuration/problems/05-load-dotenv/

    7. Requires .env in repo root (copy from .env.example)

============================================================
Goal
============================================================

Practice:

    - godotenv for local dev
    - .env vs system env

============================================================
*/

package main

func main() {}
