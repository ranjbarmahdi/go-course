# application/contracts

Purpose: Shared interfaces used by multiple use cases.

Rules:
- Interfaces only — no implementations here
- Implementations live in `infra/adapters/`

Files (Topic 12):
- `contracts.go` — MessagePublisher, RunInTx
